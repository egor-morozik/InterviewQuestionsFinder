name: Collect Interview Questions

on:
  schedule:
    - cron: '0 9 * * *'  # Каждый день в 9 утра UTC
  workflow_dispatch:      # Ручной запуск

jobs:
  collect:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: pip install requests
      
    - name: Run collector
      env:
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        QDRANT_URL: ${{ secrets.QDRANT_URL }}
        QDRANT_API_KEY: ${{ secrets.QDRANT_API_KEY }}
      run: python collector.py
      
    - name: Create GitHub summary
      run: |
        echo "# 🚀 Результаты автоматического сбора" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Дата:** $(date '+%Y-%m-%d %H:%M')" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ -f "collection_report.md" ]; then
          echo "## 📊 Краткая статистика" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Показываем первые строки отчета
          head -20 collection_report.md >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📄 [Полный отчет](artifact/collection-results/collection_report.md)" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ Отчет не сгенерирован" >> $GITHUB_STEP_SUMMARY
        fi
        
    - name: Upload results
      uses: actions/upload-artifact@v4
      with:
        name: collection-results
        path: |
          collection_report.md
          results.json